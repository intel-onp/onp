ENV {% if "http_proxy" in proxy_env %} http_proxy="{{ proxy_env.http_proxy }}" {% endif %} {% if "https_proxy" in proxy_env %} https_proxy="{{ proxy_env.https_proxy }}" {% endif %} {% if "no_proxy" in proxy_env %} no_proxy="{{ proxy_env.no_proxy }}"{% endif %}

{% if proxy_env.http_proxy %}
RUN echo 'proxy={{ proxy_env.http_proxy }}' >> /etc/yum.conf \
    && sed -i -e 's/enabled.*/enabled=0/' /etc/yum/pluginconf.d/fastestmirror.conf
{% endif %}
